UTest({
	'inlined mask': function(){
		
		return $render(`
			script type='text/mask' data-run='true' > :html > '''
				span > 'Hello'
			'''
		`)
		.done($container => {
			
			$container
				.children('span')
				.eq_('length', 1)
				.eq_('text', 'Hello')
				;
		})
	}
})